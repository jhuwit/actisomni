% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{acti_sleep_tudor_locke}
\alias{acti_sleep_tudor_locke}
\title{Summarize sleep periods with Tudor--Locke}
\usage{
acti_sleep_tudor_locke(
  data,
  time_col = "time",
  activity_col = NULL,
  sleep_col = "sleep",
  id_col = NULL,
  n_bedtime_start = 5,
  n_wake_time_end = 10,
  min_sleep_period = 160,
  max_sleep_period = 1440,
  min_nonzero_epochs = 0
)
}
\arguments{
\item{data}{Epoch-level data with timestamp, activity-count, and sleep-label
columns.}

\item{time_col}{Name of the POSIXct timestamp column.}

\item{activity_col}{Name of the non-negative activity-count column. When
\code{NULL}, the first of \code{activity}, \code{counts}, \code{axis1}, or \code{count} is used.}

\item{sleep_col}{Name of a logical sleep column or a character column using
\code{S}/\code{W} or \code{sleep}/\code{wake} labels. Use \code{\link[=acti_sleep_tudor_locke_diary]{acti_sleep_tudor_locke_diary()}} to
create this column from a sleep diary.}

\item{id_col}{Optional recording identifier column.}

\item{n_bedtime_start}{Minimum consecutive sleep epochs required to begin a
sleep period.}

\item{n_wake_time_end}{Wake epochs shorter than this are absorbed into sleep.}

\item{min_sleep_period}{Minimum retained sleep-period duration, in minutes.}

\item{max_sleep_period}{Maximum retained sleep-period duration, in minutes.}

\item{min_nonzero_epochs}{Minimum nonzero activity epochs in a retained
period.}
}
\value{
A tibble of Tudor--Locke sleep periods and metrics, as returned by
\code{\link[actigraph.sleepr:apply_tudor_locke]{actigraph.sleepr::apply_tudor_locke()}}.
}
\description{
Coerces actisomni-style epoch data to an \code{actigraph.sleepr} \code{tbl_agd} and
delegates period detection and metric calculation to
\code{\link[actigraph.sleepr:apply_tudor_locke]{actigraph.sleepr::apply_tudor_locke()}}. This avoids maintaining a separate
implementation of the Tudor--Locke rules.
}
\examples{
time <- as.POSIXct("2020-01-01", tz = "UTC") + 0:299 * 60
epochs <- data.frame(time = time, activity = 0, sleep = TRUE)
acti_sleep_tudor_locke(epochs)
}
